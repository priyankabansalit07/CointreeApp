/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BTCPipe } from './BTC.pipe';

describe('Pipe: BTCe', () => {
  it('create an instance', () => {
    let pipe = new BTCPipe();
    expect(pipe).toBeTruthy();
  });
});
